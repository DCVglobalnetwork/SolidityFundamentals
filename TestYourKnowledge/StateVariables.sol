// SPDX-License-Identifier: MIT
// This specifies the license under which the contract is released. The MIT license is a permissive license.

pragma solidity ^0.8.0;
// This sets the version of the Solidity compiler to be used. In this case, it is version 0.8.0 or newer.

contract StateVariables {
    // State variable
    // This is a state variable of type uint (unsigned integer).
    // It is stored on the blockchain and can be accessed publicly.
    // State variable
    // This is a state variable of type string.
    // It is stored on the blockchain and can be accessed publicly.
    // State variable
    // This is a state variable of type bool (boolean).
    // It is stored on the blockchain and can be accessed publicly.
    // Constructor
    // The constructor is a special function that runs only once when the contract is deployed.
    // It takes three parameters: an unsigned integer, a string, and a boolean.
    // It sets the initial values of the state variables 'myNumber', 'myString', and 'myBool'.
}
